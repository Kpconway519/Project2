{{!-- Right now, just making this a loop that goes back to the beginning for dev purposes, later it could be a link to reviews which saves a session id or something, or it could take you to the timer screen that shows you when your barber is going to be on the way, kind of like the "your pizza is being made screen at dominos" --}}
{{!--<h1>Appointment Page</h1>--}}

<!--Set time and confirm button-->
<div class="container">
<form>
    <input type="text" placeholder="put time here" name="apptTime" id="apptTime">
</form>
</div>

<a href="/confirm">
<button type="button" class="btn btn-primary btn-lg btn-block" id="setTimeConfirm">Set Time and Confirm</button>
</a>
</div>
    <!--End of time and confirm button-->


<!--Start of Side Nav-->
<div id="mySidenav" class="sidenav">
    <a href="/" id="home">Back to Home</a>
    <a href="/order" id="servicesPage">Go To Services</a>
    <a href="/barber" id="social">Go To Barbers</a>
</div>
    <!--End of Side Nav-->

<script>


$("#setTimeConfirm").on("click", function() {
      event.preventDefault()
let selectedTime = $("#apptTime").val().trim();;



if (selectedTime) {
          let timeUpdate = {
            session: `${localStorage.getItem("tempSessionId")}`,
            time: selectedTime
          };

      //$.put("appointment/barber", barbUpdate)
      $.ajax({
        method: "PUT",
        url: "/appointment/time",
        data: timeUpdate
        }).then(function(data) {
          console.log('works')
          location.href = `/confirm/${localStorage.getItem("tempSessionId")}`
        })

}else{
  alert('You must Select a Time')
}
      

})



</script>